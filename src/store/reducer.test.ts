import { reducer } from './reducer'
import { ChainState, State } from './state'

describe('reducer', () => {
  describe('PlayBlocks', () => {
    it('return an empty state if different URL', () => {})
  })
})
